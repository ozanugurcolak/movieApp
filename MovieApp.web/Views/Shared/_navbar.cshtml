<nav class="navbar navbar-expand-lg navbar-dark bg-danger">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <a href="/" class="navbar-brand">SinemaMüzesi</a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a href="/movies/list" class="nav-link">Filmler</a>
                </li>
                <li class="nav-item">
                    <a href="/admin/moviecreate" class="nav-link">Film Ekle</a>
                </li>
                <li class="nav-item">
                    <a href="/admin/movielist" class="nav-link">Admin Filmler</a>
                </li>
                <li class="nav-item">
                    <a href="/admin/genrelist" class="nav-link">Film Türleri</a>
                </li>
            </ul>

            <form action="/movies/list" method="get" class="d-flex">
                <input type="text" placeholder="Film Ara" name="q" class="form-control me-2" />
                <button type="submit" class="btn btn-outline-light mr-0">Bul</button>
            </form>

            @if (User.Identity.IsAuthenticated)
            {
                <!-- Oturum açılmışsa "Hesabım" butonu ve "Çıkış Yap" butonu görünür -->
                <a href="/UserAccount/Account" class="btn btn-outline-light ms-2">Hesabım</a>
                <a href="/UserAccount/Logout" class="btn btn-outline-light ms-2">Çıkış Yap</a>
            }
            else
            {
                <!-- Oturum açılmamışsa "Giriş Yap" butonu görünür -->
                <a href="@Url.Action("Login", "UserAccount")" class="btn btn-outline-light ms-2">Giriş Yap</a>
            }
        </div>
    </div>
</nav>
